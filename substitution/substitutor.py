# substitution/substitutor.py — Person 3's file
# Stub so imports don't break. Person 3 fills this in.

def get_substitutes(ingredient: str, top_k: int = 3) -> list[dict]:
    """
    Return ranked ingredient substitutes using FlavorDB + food2vec.
    TODO: Person 3 implements this.

    Returns:
        List of dicts with keys: substitute, similarity_score, notes
    """
    return [{"substitute": "N/A", "similarity_score": 0.0, "notes": "Substitution model not yet implemented."}]


def estimate_cost_inr(ingredients: list[str]) -> float:
    """
    Estimate total recipe cost in INR.
    TODO: Person 3 implements this.
    """
    return 0.0
